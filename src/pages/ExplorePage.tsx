// TEMPORARILY REMOVED - AudioPlayer functionality moved to SearchPage.tsx
// This file is kept for reference but the route now redirects to NotFoundPage

/*
import { useState, useEffect, useCallback, useMemo, useRef } from "react";
import { Link } from "react-router-dom";
import { Compass, Filter, Search, ChevronDown, Play, AlertCircle } from "lucide-react";
import { createPortal } from "react-dom";
import { Recording, Region, RecordingType, VerificationStatus, RecordingQuality, UserRole } from "@/types";
import { recordingService } from "@/services/recordingService";
import Pagination from "@/components/common/Pagination";
import LoadingSpinner from "@/components/common/LoadingSpinner";
import AudioPlayer from "@/components/features/AudioPlayer";

// ... rest of the file content has been moved to SearchPage.tsx
*/

export default function ExplorePage() {
  // This component is temporarily disabled
  // Route /explore now redirects to NotFoundPage
  return null;
}
